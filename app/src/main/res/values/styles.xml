<resources>

    <!-- Base application theme. -->
    <style name="ThemeAppRojo"  parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="actionBarStyle">@style/MyBar</item>
    </style>

    <style name="MyBar"
           parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="background">@color/list_background_pressed</item>
    </style>

    <style name="btn" parent="android:Widget.Holo.Light.Button">
        <item name="android:minHeight">5dp</item>
        <item name="android:minWidth">5dp</item>
    </style>

    <style name="edittext_msj" parent="android:Widget.Holo.Light.EditText">
        <item name="android:background">@android:drawable/editbox_background</item>
        <item name="android:ems">10</item>
        <item name="android:imeActionId">200</item>
        <item name="android:imeOptions">actionSend</item>
        <item name="android:inputType">text</item>
        <item name="android:layout_marginLeft">@dimen/pad_5dp</item>
        <item name="android:layout_marginRight">@dimen/pad_5dp</item>
        <item name="android:textColorHint">@color/main_color_gray</item>
        <item name="android:textSize">@dimen/txt_16sp</item>
    </style>

</resources>
